# Notebooks

Exploratory notebooks may be added here when they support a reproducible workflow. Production
data preparation, training, and evaluation logic must remain in `src/synthdet` or `scripts`.
Notebook outputs and large embedded data should be cleared before commit.

