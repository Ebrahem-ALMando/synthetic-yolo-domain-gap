# API application

Reserved for a future FastAPI inference service. Sprint 1 deliberately contains no placeholder
endpoints or business logic. Shared inference code will live in `src/synthdet/inference`.

